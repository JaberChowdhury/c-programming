#include <stdio.h>

int main()
{
  int width = 1000;
  int length = 100;
  int area;
  area = width * length;
  
  printf("Length is  %d\n", length);
  printf("Width is  %d\n", width);
  printf("Area is  %d\n", area);

  return 0;
}
